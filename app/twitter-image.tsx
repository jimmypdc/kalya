/**
 * Twitter/X card image. Reuses the same design as the Open Graph image so
 * shares on X get the identical branded 1200×630 card.
 */
export { default, alt, size, contentType } from './opengraph-image';
